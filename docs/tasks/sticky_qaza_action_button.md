# Sticky Qaza Action Button

The Qaza prayer list shows the completion action only when at least one record is selected. The action is overlaid at the bottom of the viewport so it remains accessible while the list scrolls.
