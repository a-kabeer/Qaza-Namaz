# Platform Setup

This package contains the Dart/Flutter application source and project configuration.

After extracting it, run:

```bash
flutter create .
flutter pub get
flutter test
flutter run
```

`flutter create .` generates the standard Android/iOS/web/windows/linux platform folders for the installed Flutter SDK without overwriting the Dart source in `lib/`.
