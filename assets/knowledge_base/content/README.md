# Knowledge Base Content

This directory is the source location for bundled Knowledge Base articles.

Content authors should edit structured content files here rather than Dart widgets. The exact JSON contract, required fields, validation rules, and examples will be introduced in Part 2.

Required content categories:

- `masail`
- `mugalat`

The published dataset will support Urdu and English text, references, and related articles while remaining fully offline at runtime.
